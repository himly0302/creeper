## 11.26

1. 想加一个新功能， 与目前已有功能隔离(考虑下能调整当前的项目结构吗？然后使用通用工具函数等)
```
1. 提供一个文件夹地址，获取文件夹中所有的文件内容，然后一起发给llm进行整合; 在cli指定名字表示 llm返回内容保存的文件名<假设为A>
2. 但是不同的类型在整合的时候需要保留的内容不一样，就需要提供给llm不同的提示词； 因此先预设一批提示词, 通过cli中 某个参数 来获取其对应的提示词, 和 文件内容一起发给llm;
3. 通过redis来映射 A 和 文件夹的路径地址以及获取了内容的文件名 , 那么再次提供此文件夹地址时，只有新的文件才会获取内容合并后发给llm进行整合，然后llm返回的内容再和A的内容合并发给llm，最终返回的内容用于更新文件A
4. 不考虑删除文件的情况
```
2. redis 本地持久化
3. 图片爬取,  要与目前已有功能隔离，只使用通用的工具函数


## 11.27

新切一个分支，然后调整 “文件夹内容 LLM 整合”功能点， 改成 “对文件进行解析”
1. 提供一个文件夹地址，获取文件夹中每个文件的内容，然后独自发给llm; 在cli指定路径 表示这些文件通过llm处理后生成文件所在的 文件夹(每个输入文件，对应一个输出文件)地址
2. 通过redis 来过滤已经处理过的文件


## 提示词调整
parsed 文档就是 本地资讯分析专家 这个提示词在 deepseek chat 中生成的 文档；你觉得 这个提示词咋样？如果让你处理，你会保留什么信息？
按照你的想法，对本地资讯分析专家提出改进建议; 只提修改建议，不改原提示词

## 12.01[未处理]
* "请点击下方方框继续操作，以确认您不是机器人"  遇到这种情况，打开弹窗主动验证
* 重复间隔实践太长了
* 